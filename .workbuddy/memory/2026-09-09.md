# 2026-09-09 工作日志

## 开发环境全量配置与验证（知行智学）

完成本机开发工具链补全与项目运行环境配置，全部通过命令行实测：

- **local-hub（Python 后端）**：`.python-version` 原先固定 3.11 与 pyproject `>=3.12,<3.13` 冲突，已 `uv python pin 3.12` 修正；`uv sync` 建 .venv（曾遇 colorama RECORD/拒绝访问错误，重跑一次即恢复）。验证：import ok、ruff 全过、pytest 70 项全过。注意：包未安装进 venv（`tool.uv package=false`），运行必须 `PYTHONPATH=src` 或用 scripts/dev.ps1 / run_pc_workbench.ps1。
- **pc-workbench（前端）**：pnpm 11.25.0 + Node 22.22.2，`pnpm install` 完成；vite 8.2.2 / tsc 5.9.3 / vitest 3.2.7；lint 与测试通过。
- **运行验证**：hub（127.0.0.1:8777，`/api/dashboard` 返回正常 JSON）+ vite（localhost:5173，200）同时在线。
- **Android SDK（新装）**：官方脚本 install-android-sdk.ps1 走代理下载失败，改为手动：cmdline-tools 经代理 curl 下载成功（153MB）；platform-tools/build-tools 37.0.0/platforms android-37.0 改走**腾讯镜像 mirrors.cloud.tencent.com/AndroidSDK**（秒级完成，远快于代理）。SDK 位于 `%LOCALAPPDATA%\Android\Sdk`，adb 37.0.1 实测可用。已写入用户级 ANDROID_HOME/ANDROID_SDK_ROOT/JAVA_HOME(C:\Program Files\Java\jdk-21)/Path。
- **Gradle**：项目要求 compileSdk 37/build-tools 37.0.0（注意：install-android-sdk.ps1 里写的 android-34 已过时）；gradle-9.6.1 通过腾讯镜像 mirrors.cloud.tencent.com/gradle 完成首次拉取并缓存，JDK 21 启动验证通过。用户级 `~/.gradle/gradle.properties` 已配 127.0.0.1:7897 代理供 google maven 拉依赖。
- Android 工程已写 local.properties 指向 SDK；构建须从 Junction 路径 C:\ZhixingZhixue 进行（中文路径会炸 Gradle/Kotlin Worker）。

### 经验
- 国内环境装 Android SDK 最快路径：cmdline-tools 走代理，其余组件直接下腾讯镜像 zip 手动解压到标准目录（build-tools zip 内目录名 android-XX 需改名为版本号如 37.0.0），无需 sdkmanager。
- sdkmanager 的平台包名是 `platforms;android-37.0` 而非 `android-37`。
