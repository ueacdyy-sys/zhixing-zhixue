# 贡献指南

> 欢迎为知行智学贡献代码、文档和研究建议

## 一、项目当前状态

**已完成**: 采集层(PC窗口+Android RTSP) + 数据处理层(事件结构+时间轴+质量门控)  
**进行中**: 证据卡基础逻辑  
**待实现**: 功能层核心(兴趣流采集+机会窗识别+兴趣-知识桥接+AI解释)

**核心挑战**: 学习机会窗识别算法(WindowScore评分公式)尚在研究阶段,需要结合认知负荷理论、心流理论和短视频推荐机制。

详见 [系统概览](docs/系统概览.md)。

---

## 二、贡献方向

### 优先级P0(核心创新,急需贡献)

**1. 学习机会窗识别算法研究**
- 认知负荷理论在学习时机判断中的应用
- 心流理论与最优学习体验状态识别
- 借鉴短视频推荐算法的行为反馈机制
- WindowScore评分公式设计建议
- 窗口类型识别方法(启动/卡顿/策略切换/恢复/复盘)

**2. 兴趣流特征工程**
- PC端兴趣特征提取(浏览历史、搜索词、收藏)
- 手机端行为特征(停留、复看、主动提问)
- 兴趣强度计算方法

**3. 兴趣-知识桥接规则**
- 兴趣标签→知识点映射规则设计
- 微学习任务生成模板
- 任务可接受性判断标准

### 优先级P1(重要增强)

**4. 证据卡模板设计**
- 复盘报告结构设计
- 学习者视角的可读性优化
- 教师视角的批注界面设计

**5. AI解释Prompt工程**
- 证据引用约束的Prompt设计
- 三条禁止规则的执行机制
- 多模型测试和效果对比

**6. 质量门控规则优化**
- 降级边界条件优化
- 新模态(视频/EEG)的质量评估规则

### 优先级P2(可选增强)

**7. 多模态接入**
- OpenCV/MediaPipe视频分析模块
- BrainFlow/MNE生理信号处理
- 眼动追踪数据接入

**8. 前端交互优化**
- 时间线可视化增强
- 证据卡查看体验优化
- 教师复核界面原型

---

## 三、技术贡献流程

### 前置要求

- **Node.js**: 22+ 
- **pnpm**: 11.25+
- **Python**: 3.12+
- **uv**: 最新版本

### 提交前检查(强制)

```powershell
# 1. 仓库卫生检查
python tools/verify_repository_hygiene.py

# 2. 文本编码检查
python tools/verify_text_encoding.py

# 3. PC工作台代码质量
cd apps\pc-workbench
pnpm lint
pnpm test

# 4. 本地中枢代码质量
cd services\local-hub
uv run ruff check src tests
uv run pytest -v
```

所有检查必须通过才能提交。

### 代码风格

**TypeScript/React**
- 使用ESLint 10配置
- 遵循React 19最佳实践(函数组件+Hooks)
- 类型定义清晰完整
- 组件职责单一

**Python**
- 遵循ruff 0.9规则
- 类型注解使用 `from __future__ import annotations`
- 测试覆盖关键路径(目标覆盖率>80%)
- 文档字符串清晰准确

### 提交消息规范

使用清晰的中文提交消息:

```
简短描述修改内容(50字内)

详细说明:
- 修改了什么
- 为什么要修改
- 影响范围

相关Issue: #123
```

示例:
```
实现学习机会窗识别的基础评分框架

- 新增WindowScore计算模块(learning/window_score.py)
- 实现兴趣强度和知识相关度两个维度
- 添加15项单元测试

待完成: 状态可接受度和打扰成本维度

相关Issue: #45
```

---

## 四、隐私与安全约束(不可违反)

### 采集边界(绝对禁止)

**PC端不得采集**:
- ❌ 屏幕截图
- ❌ 键盘输入
- ❌ 剪贴板内容
- ❌ 浏览历史完整URL(仅允许域名级别)

**手机端不得**:
- ❌ 自动化控制第三方App
- ❌ 在用户未授权时采集媒体
- ❌ 上传到云端(必须使用`local://`URI)

### 证据分级约束(强制执行)

**手机单帧/OCR/无音频/时间不连续内容**:
- 只能标记为`CANDIDATE_ONLY`
- 不得生成兴趣结论、知识诊断、学习任务推荐、强制干预

**可穿戴设备数据**:
- 禁止能力判断(ability_claim)
- 禁止注意力诊断(attention_diagnosis)
- 禁止医学判断(medical_claim)
- 禁止心理判断(psychological_claim)

### 禁止内容(提交前检查)

不得在源码中加入:
- 广告SDK或代码
- 云分析/统计服务
- 上游信令或平台私有接口
- 自动化控制第三方App的逻辑
- 未经授权的媒体采集代码

### Git提交约束

不得提交:
- SDK、虚拟环境、node_modules
- 采集的证据文件(*.mp4, *.mkv, *.json)
- 模型文件(*.pt, *.onnx, *.bin)
- APK或本机配置(`local.properties`)
- 学生数据或设备标识

---

## 五、特殊构建约束

### Windows Android构建

移动端Windows构建必须从ASCII路径启动(如`C:\ZhixingZhixue\mobile-edge\third_party\screenstream_source`),避免Gradle/Kotlin Worker中文路径解码错误。

项目根目录可能包含中文路径,使用Junction创建ASCII别名:
```powershell
mklink /J C:\ZhixingZhixue "C:\Users\86152\Desktop\zhixing-zhixue"
```

---

## 六、文档贡献

### 文档结构要求

所有文档必须按照**用户视角-功能视角-技术视角**三层结构编写:

1. **用户视角**: 为谁解决什么问题,适用场景,用户价值
2. **功能视角**: 系统能做什么,实现状态(✅/🚧/⏳),功能边界
3. **技术视角**: 如何实现,技术栈,架构设计,数据流

参考 [系统概览](docs/系统概览.md) 的结构。

### 文档更新时机

- 新增功能模块时,同步更新相关文档
- 修改核心算法时,更新技术视角说明
- 调整功能优先级时,更新功能视角实现状态

---

## 七、测试要求

### 必须添加测试的场景

- 新增数据验证逻辑
- 新增质量门控规则
- 新增证据分级逻辑
- 新增API端点
- 修改核心算法

### 测试覆盖目标

- 单元测试: 关键函数和类 >80%覆盖率
- 集成测试: 端到端验证链完整通过
- 边界测试: 异常输入、降级场景、错误处理

---

## 八、获取帮助

- 阅读 [系统概览](docs/系统概览.md) 理解整体架构
- 查看 [环境配置](docs/环境配置与验收总览.md) 解决环境问题
- 查看 [审查入口](docs/审查入口.md) 了解验收标准
- 提交Issue时附上:
  - 问题描述
  - 复现步骤
  - 环境信息(操作系统、Python版本、Node版本)
  - 相关日志

---

## 九、许可与版权

(待添加)

---

**最后更新**: 2026-09-09  
**维护者**: 项目负责人

感谢你的贡献!
