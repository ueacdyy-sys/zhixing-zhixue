# 知行智学环境配置与验收总览

**验收时间**：2026-07-16  
**工程根目录**：`C:\Users\Administrator\Desktop\知行智学`

## 1. 验收结论

| 环境 | 状态 | 实测证据 |
|---|---|---|
| Python 本地中枢 | 通过 | Python 3.11.9；`ruff` 通过；`pytest`：1 passed；包导入通过 |
| PC 工作台前端工具链 | 通过 | Node 24.11.0、pnpm 11.4.0、Vite 7.3.6、Vitest 3.2.7、TypeScript 5.9.3 均可执行 |
| JDK/Gradle | 通过 | JDK 17.0.19；Gradle 9.5.1 使用该 JDK 启动 |
| Rust/.NET | 可用 | Rust 1.95.0；.NET SDK 10.0.300 |
| Spec Kit | 通过 | `specify 0.12.16`；Constitution、规格和任务清单存在 |
| Android SDK/adb | 通过 | Android Studio、Command-line Tools 19.0、Platform-Tools 37.0.0、Android 35、Build-Tools 35.0.0 已实测 |

## 2. 当前工程环境

### 本地中枢

目录：`services/local-hub/`

- 隔离环境：`.venv`
- 依赖：pytest、pytest-cov、ruff
- 运行入口：`scripts/dev.ps1`
- Windows 中文路径兼容策略：不使用 editable 安装；测试通过 `src` 路径加载包。

验证命令：

```powershell
cd C:\Users\Administrator\Desktop\知行智学\services\local-hub
uv run ruff check src tests
uv run pytest
.\scripts\dev.ps1 python -c "import zhixingzhixue_hub"
```

### PC 工作台

目录：`apps/pc-workbench/`

- pnpm workspace 已建立。
- 已安装 React、Vite、Vitest、TypeScript、ESLint 与 jsdom。
- 当前只完成构建/测试环境，不代表已实现或可打包工作台界面。

验证命令：

```powershell
cd C:\Users\Administrator\Desktop\知行智学
pnpm --filter @zhixingzhixue/pc-workbench exec vite --version
pnpm --filter @zhixingzhixue/pc-workbench exec vitest --version
pnpm --filter @zhixingzhixue/pc-workbench exec tsc --version
```

### Android 手机端

目录：`apps/mobile-android/`

JDK、Gradle、Android SDK 命令行工具均已可用。已写入用户级 `ANDROID_SDK_ROOT`、`ANDROID_HOME`、`JAVA_HOME` 和 PATH，并在本目录写入 `local.properties`。

```powershell
adb version
sdkmanager --version
echo $env:ANDROID_SDK_ROOT
```

推荐环境变量：

```powershell
$env:ANDROID_SDK_ROOT = '<实际 Android SDK 路径>'
$env:ANDROID_HOME = $env:ANDROID_SDK_ROOT
$env:Path += ";$env:ANDROID_SDK_ROOT\platform-tools;$env:ANDROID_SDK_ROOT\cmdline-tools\latest\bin"
```

随后安装并验收至少一套 Android SDK Platform、Build-Tools、Platform-Tools 和 Command-line Tools；具体 API Level 在手机端工程生成时与目标手机系统版本一并冻结。

## 3. Spec Kit 与 TDD

- SDD 工件位于 `specs/001-multi-entry-learning-evidence/`。
- TDD 执行顺序位于 `tasks.md` 与 `docs/tdd/test-matrix.md`。
- 第一条实现主线仍是手机日常碎片化内容场景；PC 从第一阶段承担分析中控与证据工作台，PC 学习实践为并行场景。

## 4. 复测纪律

每次新增 SDK、前端打包器或设备驱动后，先更新本文件的版本、路径和实测命令，再允许在规格中标记为“可用”。未通过命令行验证的环境只能标记为“已安装待验收”。
