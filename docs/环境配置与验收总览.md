# 知行智学环境配置与验收总览

> 完整的开发环境配置指南与验收标准

**最近验收时间**：2026-09-09  
**工程根目录**：`C:\Users\86152\Desktop\zhixing-zhixue`  
**依赖版本**：已更新至2026年9月最新稳定版本

## 概述(三视角)

**用户视角**: 本文档为开发者`提供完整的环境配置指南,确保能快速启动PC工作台和本地中枢
`**`功能视角`**`: 覆盖Python后端、React前端、An`droid构建、Rust/.NET工具链的完整配置  
**技术视角**: 提供验收命令和实测证据,所有环境均已通过验证

---

## 1. 验收结论

| 环境              | 状态 | 当前版本                                                                         | 实测证据                                                                          |
| --------------- | -- | ---------------------------------------------------------------------------- | ----------------------------------------------------------------------------- |
| Python 本地中枢     | 通过 | Python 3.12.12, pytest 9.1, ruff 0.9（uv 管理，.python-version=3.12）             | `ruff check` 通过；`pytest` 70项测试通过；包导入通过（需 `PYTHONPATH=src` 或用 scripts/dev.ps1） |
| PC 工作台前端工具链     | 通过 | Node 22+, pnpm 11.25, React 19.2.7, Vite 8.2, TypeScript 5.9.2, ESLint 10.10 | Vite dev server正常启动；测试通过                                                      |
| JDK/Gradle      | 通过 | JDK 21.0.9；Gradle 9.6.1（wrapper）                                             | Gradle 使用配置的 JDK 启动（`gradlew --version` 通过）                                   |
| Rust/.NET       | 可用 | Rust 1.95+（可选，未安装）；.NET SDK 8.0                                              | 编译器可执行                                                                        |
| Spec Kit        | 可选 | specify CLI（未安装，规格工件在库内即可）                                                   | Constitution、规格和任务清单存在                                                        |
| Android SDK/adb | 通过 | cmdline-tools 19+, Platform-Tools 37.0.1, Build-Tools 37.0.0, Android 37.0   | `adb version` 实测可用                                                            |

## 2. 当前工程环境

### 本地中枢

目录：`services/local-hub/`

- Python 版本：3.12 (已更新)
- 隔离环境：`.venv`
- 依赖：pytest 9.1+、pytest-cov、ruff 0.9+
- 测试覆盖：70项集成和单元测试
- 运行入口：`python -m zhixingzhixue_hub.pc.workbench_server`
- Windows 中文路径兼容策略：通过 `PYTHONPATH` 和 `src` 目录结构确保模块导入。

核心模块：

- PC任务工作台 (`pc/task_workbench.py`, `pc/workbench_runtime.py`, `pc/workbench_server.py`)
- 前台窗口采集 (`pc/foreground_capture.py`)
- 手机证据接入 (`phone/interest_session.py`, `phone/keyframe_ocr_candidate.py`)
- 证据卡片构建 (`evidence/card_builder.py`)
- 质量门控 (`quality/modality_gate.py`)
- 时间线对齐 (`timeline/aligner.py`)
- 两条验证链 (`validation_chains.py`)

验证命令：

```powershell
cd services\local-hub
uv run ruff check src tests
uv run pytest -v
uv run python -c "import zhixingzhixue_hub; print('导入成功')"
```

### PC 工作台

目录：`apps/pc-workbench/`

- 前端框架：React 19.2.7 (最新稳定版)
- 构建工具：Vite 8.2.0 (Rolldown架构)
- 语言：TypeScript 5.9.2
- 测试：Vitest 3.2.4 + Testing Library
- Lint：ESLint 10.10.0
- pnpm workspace 已建立

功能实现：

- PC 独立学习任务入口：用户可直接在PC创建学习任务,无需手机前置
- 实时仪表盘：显示当前任务状态、采集状态、质量标记
- 时间线视图：展示前台窗口事实的可回放时间线
- 跨端联查：展示已关联的手机候选证据(CANDIDATE_ONLY状态)
- 隐私保护：仅采集窗口标题和进程元数据,不截图、不记录键盘/剪贴板/浏览历史

API代理配置：开发环境代理到 `http://127.0.0.1:8777` (本地中枢)

验证命令：

```powershell
cd apps\pc-workbench
pnpm exec vite --version  # 应显示 8.x
pnpm exec vitest --version  # 应显示 3.x
pnpm exec tsc --version  # 应显示 5.9.x
pnpm lint  # ESLint检查
pnpm test  # 运行测试
pnpm dev   # 启动开发服务器
```

### Android 手机端

目录：`mobile-edge/third_party/screenstream_source/`

当前实现：RTSP媒体传输内核,仅在用户主动授权MediaProjection后传输媒体。

核心约束：

- 不控制第三方App,不自动滑动或播放
- 单帧/OCR、无同源音频或时间不连续媒体只能输出 `CANDIDATE_ONLY`
- 不包含广告、Firebase、云分析或上游商店变体

JDK、Gradle、Android SDK 命令行工具均已可用。已写入用户级 `ANDROID_SDK_ROOT`、`ANDROID_HOME`、`JAVA_HOME` 和 PATH。

Windows构建特殊要求：

- 必须从ASCII路径构建,避免Gradle/Kotlin Worker中文路径解码错误
- 建议使用Junction: `C:\ZhixingZhixue` → 实际项目路径
- `local.properties` 配置SDK路径,该文件不纳入版本控制

验证命令：

```powershell
adb version
# 从Junction路径构建
cd C:\ZhixingZhixue\mobile-edge\third_party\screenstream_source
.\gradlew.bat --offline :learning-domain:test :app:assembleDebug
```

推荐环境变量：

```powershell
$env:ANDROID_SDK_ROOT = 'mobile-edge\tools\android-sdk'
$env:ANDROID_HOME = $env:ANDROID_SDK_ROOT
$env:Path += ";$env:ANDROID_SDK_ROOT\platform-tools;$env:ANDROID_SDK_ROOT\cmdline-tools\latest\bin"
```

## 3. Spec Kit 与 TDD

- 当前冻结 SDD 工件位于 `specs/004-realtime-learning-core/`；001–003 为已废弃历史规格，不得用于实现或验收。
- TDD 执行顺序以该冻结规格下的契约与任务为准。
- 手机公开媒体连续流、眼镜第一视角流与 PC 学习流均是独立主链；PC 同时承担本地分析中枢与证据工作台。

## 4. 复测纪律

每次新增 SDK、前端打包器或设备驱动后，先更新本文件的版本、路径和实测命令，再允许在规格中标记为“可用”。未通过命令行验证的环境只能标记为“已安装待验收”。

## 5. 国内镜像源配置（2026-09-09 起生效）

| 工具                     | 镜像配置                                                                      | 位置                            |
| ---------------------- | ------------------------------------------------------------------------- | ----------------------------- |
| npm/pnpm               | `registry=https://registry.npmmirror.com`                                 | 仓库根 `.npmrc`                  |
| uv                     | PyPI 清华镜像 + Python 发行包 npmmirror 镜像                                       | `%APPDATA%\uv\uv.toml`（用户级）   |
| pip                    | `index-url=https://pypi.tuna.tsinghua.edu.cn/simple`                      | `%APPDATA%\pip\pip.ini`（用户级）  |
| Gradle 发行包             | `mirrors.cloud.tencent.com/gradle/gradle-9.6.1-bin.zip`                   | `gradle-wrapper.properties`   |
| Maven 依赖               | 阿里云 `maven.aliyun.com/repository/{google,central,gradle-plugin}` 优先，官方库兜底 | `settings.gradle.kts`         |
| Android SDK 组件         | 腾讯云 `mirrors.cloud.tencent.com/AndroidSDK` 直链下载解压                         | `install-android-sdk.ps1`     |
| Gradle google-maven 拉取 | 用户级代理 `127.0.0.1:7897`（可选兜底）                                              | `~/.gradle/gradle.properties` |

Android SDK 重装/新机器：直接运行仓库根目录 `install-android-sdk.ps1`（国内直连，无需代理），自动安装 cmdline-tools + platform-tools + build-tools 37.0.0 + android-37.0 并写入用户级 `ANDROID_HOME`/`ANDROID_SDK_ROOT`/`JAVA_HOME`/PATH。
